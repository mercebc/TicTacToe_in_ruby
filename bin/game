#!/usr/bin/env ruby
# Allows you to require any file in lib
$: << File.join(File.dirname(__FILE__), '..', 'lib')

require 'core/menu'

class Game
  attr_reader :ui, :menu

  def initialize
    @ui = UI.new
    @menu = Menu.new(@ui)
    @menu.setup
  end
end

Game.new
